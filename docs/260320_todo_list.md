1. URSUSSolver_GH.cs에 유저가 직접 넣어야하는 인풋을 가능한 한 줄여야 함.
- api key들은 어쩔 수 없다. 이것만큼은 반드시 유저가 직접 넣어야 함.
- 주소를 지정하는 방식은 개선이 필요하나 당장은 어렵다. 그러나 이것이 지금의 기획으로선 굳이 인풋으로 받을 필요는 없으므로 그냥 기본값을 지정해주고 인풋 파라미터에선 뺄 것.
- mappingJsonPath를 굳이 유저가 지정해야할 이유를 모르겠다. 누가 이 플러그인을 이용하든 자동으로 어딘가에 캐싱을 해두면 될 일 같은데...저장 위치에 대한 고민이 필요하다.
- 왜 캐싱이 중요하냐면 지금은 avgIncome만 다루고 있지만 앞으로 여러 데이터들을 더 불러와서 weighted overlay 방식으로 Visualizer_GH.cs의 values 인풋에 넣을 것이기 때문. 좀 더 정확히 말하면 인풋 파라미터에 어떤 데이터들을 weighted overlay 방식으로 섞을 것인지 선택할 수 있게 할 것이다. 다중 데이터를 다루려면 매 동작마다 api key로 http 통신 하는 건 바보 짓이다. 컴포넌트 실행시 한번만 받아오고 나머지는 캐싱해두고 쓰는게 훨씬 빠를 것이기에 캐싱과 관련한 체계가 중요하다. best practice를 찾아볼 것.
2. 전반적인 성능은 괜찮은 것 같다. 두 컴포넌트 합쳐서 12초 정도면 준수한 편.
3. 시각화와 함께 metric을 어떻게 정리해서 유저에게 전달할 것인지에 대한 고민도 필요하다. 그냥 excel로 전달하는게 나을지?
4. 다른 건 다 api key로 불러올 수 있다지만 /home/keonchae/URSUS/refs/sheets/KIKmix.20240201.xlsx 이거는 그냥 어디선가 구해서 로컬에 갖고 있는 것 외에는 방법이 없다. 혹은 이것도 json으로 바꾸면 데이터 교환이 더 가벼워질지?
5. 아무튼 그래서 이것을 진짜 플러그인으로 배포하려면 일단 /home/keonchae/URSUS/refs/sheets/KIKmix.20240201.xlsx 이 파일을 어떤 식으로 들고 있을지에 대한 고민이 필요하다. 더불어 @GH_SETUP_GUIDE.md#L174-178 여기서 dll들을 해당 위치로 옮겨주는 동작도 필요하다. 뭐...똑똑한 유저는 자기가 알아서 하겠지만, 내가 원하는 것은 최대한의 자동화다. 해당 동작을 .msi파일로 만들고 실행 시 관리자 권한을 요청하도록 하면 가능할지 모르겠다.
